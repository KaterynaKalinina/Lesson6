describe('createMonster function', () => {
    it('should create monster object', () => {
        const monster = {
            id:1,
            userId:1,
            name: 'Elder',
            
        }
    })
});
describe('Math', () => {
    it ('should do pow', () => {
        expect(1).toBe(1);
    });
})